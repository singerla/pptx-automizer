import Automizer from './automizer';

export default Automizer;

import {
  setSolidFill,
  setText,
  setPosition,
  setAttribute,
  setChartData,
  setChartVerticalLines,
  dump,
} from './helper/modify';

export const modify = {
  setSolidFill,
  setText,
  setPosition,
  setAttribute,
  setChartData,
  setChartVerticalLines,
  dump,
};
