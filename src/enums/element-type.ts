export enum ElementType {
  Chart = 'Chart',
  Image = 'Image',
  Shape = 'Generic',
  OLEObject = 'OLEObject', 
  Hyperlink = 'Hyperlink',
}

export enum ElementSubtype {
  chart = 'chart',
  chartEx = 'chartEx',
  oleObject = 'oleObject',  
  hyperlink = 'hyperlink',
}
