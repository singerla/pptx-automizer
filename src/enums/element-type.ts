export enum ElementType {
  Chart = 'Chart',
  Image = 'Image',
  Shape = 'Generic',
  OLEObject = 'OLEObject', 
}

export enum ElementSubtype {
  chart = 'chart',
  chartEx = 'chartEx',
  oleObject = 'oleObject',  
}
